<svg xmlns="http://www.w3.org/2000/svg" width="960" height="366" viewBox="0 0 960 366" role="img" aria-labelledby="t d">
  <title id="t">문서마다 역할을 선언한다</title>
  <desc id="d">AGENTS.md는 AI가 작업 하나를 수행하는 절차를, ai-collaboration.md는 여러 작업자와 AI가 동시에 일할 때의 조직을 맡고 서문에서 서로의 역할을 선언한다. 두 절차 문서는 규칙 문서와 설계 · 계약 문서를 따른다. README와 절차 안내는 실행 방법만 담고, 규칙은 정의하지 않고 규칙 문서를 링크만 한다.</desc>
  <style>
    /*
     * 공통 도해 문법 — docs/diagrams.md 참조. 9장이 같은 규칙을 쓴다.
     *
     * 색은 "무엇인가"가 아니라 "어떤 역할인가"를 말한다.
     *   g 초록  코드가 실제로 바뀌는 단계
     *   v 보라  사람이 기준을 정하거나 확인하는 지점 (문서 · 판정 · 리뷰 · 게이트)
     *   t 주황  경계 — 들어오고 나가는 지점, 그리고 되돌아간 비용
     *   q 회색  맥락. 이 그림의 주인공이 아닌 것
     * 한 칸은 면 · 선 · 글자를 같은 색 한 벌로 쓴다. 색이 역할이므로 강조용 테두리를
     * 따로 두지 않는다.
     *
     * 설명은 칸이 아니라 선과 여백이 한다. 칸 안에는 이름 한 줄만 넣고, 무엇이
     * 흐르는지는 선 위 이름표로, 그 칸이 무슨 일을 하는지는 칸 아래 작은 글로 쓴다.
     */
    :root {
      --ink:#23211d; --ink2:#57534a; --hair:#cec8bb; --arrow:#a39c91;
      --g-fill:#e7f0ea; --g-line:#4e8f74; --g-text:#1d6b4f;
      --v-fill:#ecebf4; --v-line:#7d74ab; --v-text:#544a87;
      --t-fill:#f7ebe3; --t-line:#c07a4e; --t-text:#9c4a15;
      --q-fill:#f4f1ea; --q-line:#cec8bb; --q-text:#57534a;
    }
    @media (prefers-color-scheme: dark) {
      :root {
        --ink:#edeae3; --ink2:#a8a199; --hair:#3f3b35; --arrow:#6f685f;
        --g-fill:#1d2a24; --g-line:#3f7a5f; --g-text:#4fbe8f;
        --v-fill:#24222e; --v-line:#6a5f96; --v-text:#a396d8;
        --t-fill:#2c211a; --t-line:#8f5c38; --t-text:#d98b52;
        --q-fill:#2a2724; --q-line:#4a453e; --q-text:#a8a199;
      }
    }
    text { font-family: Pretendard, "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", system-ui, sans-serif; }

    .n { stroke-width:1.5; }
    .n.g { fill:var(--g-fill); stroke:var(--g-line); }
    .n.v { fill:var(--v-fill); stroke:var(--v-line); }
    .n.t { fill:var(--t-fill); stroke:var(--t-line); }
    .n.q { fill:var(--q-fill); stroke:var(--q-line); }

    .l { font-size:15px; font-weight:700; }
    .l.g { fill:var(--g-text); } .l.v { fill:var(--v-text); }
    .l.t { fill:var(--t-text); } .l.q { fill:var(--q-text); }
    .l.sm { font-size:13.5px; }

    .e { fill:none; stroke:var(--arrow); stroke-width:1.8; }
    .e.d { stroke-dasharray:5 4; }
    .e.t { stroke:var(--t-line); stroke-width:1.4; }
    .ah { fill:var(--arrow); }
    .el { font-size:13.5px; fill:var(--ink2); }

    .sub { font-size:13px; fill:var(--ink2); }
    /* 그림이 가리키는 한 곳을 이름으로 부를 때. 가리키는 칸과 같은 색을 쓴다 */
    .sub.g { fill:var(--g-text); font-weight:700; }
    .sub.v { fill:var(--v-text); font-weight:700; }
    .sub.t { fill:var(--t-text); font-weight:700; }
    .hd { font-size:16px; font-weight:700; fill:var(--ink); }
    .grp { fill:none; stroke:var(--hair); stroke-width:1.2; stroke-dasharray:5 4; }
    .rule { stroke:var(--hair); stroke-width:1; }

    .bar.g { fill:var(--g-text); } .bar.t { fill:var(--t-text); } .bar.q { fill:var(--q-line); }
    .tick { font-size:12.5px; fill:var(--ink2); }
    .grid { stroke:var(--hair); stroke-width:1; stroke-dasharray:2 4; }
  </style>
  <defs>
    <marker id="a" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
      <path class="ah" d="M0 0 L10 5 L0 10 Z"/>
    </marker>
  </defs>
  <text class="sub" x="305" y="44" text-anchor="middle">AI가 작업 하나를 수행하는 절차</text>
  <text class="sub" x="655" y="44" text-anchor="middle">여러 작업자가 동시에 일할 때의 조직</text>
  <rect class="n g" x="210" y="58" width="190" height="40" rx="3"/>
  <text class="l g" x="305" y="83.3" text-anchor="middle">AGENTS.md</text>
  <rect class="n g" x="560" y="58" width="190" height="40" rx="3"/>
  <text class="l g sm" x="655" y="82.7" text-anchor="middle">ai-collaboration.md</text>
  <text class="sub" x="480" y="78" text-anchor="middle">서로의 역할을 선언</text>
  <path class="e d" d="M400 88 H560"/>
  <path class="e" d="M305 98 V132"/>
  <path class="e" d="M655 98 V132"/>
  <path class="e" d="M305 132 H655"/>
  <text class="sub" x="480" y="126" text-anchor="middle">따른다</text>
  <path class="e" d="M305 132 V160" marker-end="url(#a)"/>
  <path class="e" d="M655 132 V160" marker-end="url(#a)"/>
  <rect class="n v" x="210" y="166" width="190" height="40" rx="3"/>
  <text class="l v" x="305" y="191.3" text-anchor="middle">규칙 문서</text>
  <rect class="n v" x="560" y="166" width="190" height="40" rx="3"/>
  <text class="l v" x="655" y="191.3" text-anchor="middle">설계 · 계약 문서</text>
  <text class="sub" x="305" y="226" text-anchor="middle">코드 컨벤션 · 협업 규칙 · 담당 영역</text>
  <text class="sub" x="655" y="226" text-anchor="middle">API 계약 · ERD · ADR</text>
  <rect class="n q" x="210" y="272" width="190" height="40" rx="3"/>
  <text class="l q sm" x="305" y="296.7" text-anchor="middle">README · 절차 안내</text>
  <text class="sub" x="305" y="332" text-anchor="middle">실행 · 빌드 · Docker</text>
  <path class="e d" d="M305 272 H176 V186 H204" marker-end="url(#a)"/>
  <text class="sub" x="418" y="297" text-anchor="start">링크만 한다 · 규칙을 새로 정의하지 않는다</text>
</svg>
